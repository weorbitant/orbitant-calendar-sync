# ============================================
# Google OAuth 2.0 Credentials
# ============================================
# Obtener desde: https://console.cloud.google.com/apis/credentials

# Client ID de OAuth 2.0
GOOGLE_CLIENT_ID=tu-client-id.apps.googleusercontent.com

# Client Secret de OAuth 2.0
GOOGLE_CLIENT_SECRET=tu-client-secret

# URI de redirección (debe coincidir con el configurado en Google Cloud Console)
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# Scopes de Google Calendar (incluir userinfo.email para obtener email del usuario)
GOOGLE_SCOPES=https://www.googleapis.com/auth/calendar.readonly,https://www.googleapis.com/auth/userinfo.email

# ID del calendario (usar 'primary' para el calendario principal del usuario)
GOOGLE_CALENDAR_ID=primary

# ============================================
# Token Encryption (REQUERIDO)
# ============================================
# Clave de encriptacion para tokens OAuth (64 caracteres hex)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY=

# ============================================
# Slack Bot (Socket Mode)
# ============================================

# App-Level Token (xapp-...) - Settings > Basic Information > App-Level Tokens
# Requiere scope: connections:write
SLACK_APP_TOKEN=xapp-...

# Bot User OAuth Token (xoxb-...) - Settings > OAuth & Permissions
SLACK_BOT_TOKEN=xoxb-...

# Channel ID para notificaciones (opcional)
SLACK_CHANNEL_ID=C...

# Slack User IDs de administradores (separados por coma)
# Los admins pueden ver el boton de sincronizacion manual
SLACK_ADMINS=U12345678,U87654321

# ============================================
# Server Configuration
# ============================================

# URL base de la aplicación
SERVICE_BASE_URL=http://localhost:3000

# Puerto del servidor
PORT=3000

# ============================================
# Database (SQLite)
# ============================================

# Path to SQLite database file
DATABASE_PATH=./data/calendar.db

# ============================================
# Sync Configuration
# ============================================

# Cron expression for automatic sync (default: every 15 minutes)
# Format: second minute hour day-of-month month day-of-week
SYNC_CRON=0 */15 * * * *

# Run sync on server startup (true/false)
SYNC_ON_STARTUP=true
